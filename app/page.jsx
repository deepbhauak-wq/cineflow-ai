"use client";
import React, { useState } from "react";

export default function CineFlow() { const [u, setU] = useState(false); const [p, setP] = useState("योद्धा मंदिर खोज रहा है।"); const [done, setDone] = useState(false); const [eid, setEid] = useState(null); const [etxt, setEtxt] = useState(""); const [scs, setScs] = useState([{ id: 1, t: "Scene 1", desc: "योद्धा जंगल में है।" }, { id: 2, t: "Scene 2", desc: "मंदिर का द्वार खुला।" }]); if (!u) return (<div className="min-h-screen bg-[#070b14] text-white flex items-center justify-center p-4"><div className="w-full max-w-xs bg-[#0b1222] p-5 rounded-2xl border border-slate-800 space-y-3 text-center"><h2 className="text-sm font-black text-cyan-400">🎬 CineFlow Pro</h2><button onClick={() => setU(true)} className="w-full py-2 bg-cyan-600 font-bold text-xs rounded-lg">Enter Studio</button></div></div>); return (<div className="min-h-screen bg-[#070b14] text-slate-100 p-4 max-w-md mx-auto space-y-3"><div className="flex justify-between items-center border-b border-slate-800 pb-2"><h1 className="text-xs font-black text-cyan-400">CINEFLOW PRO</h1><button onClick={() => setU(false)} className="text-[10px] bg-red-950 text-red-400 px-2 py-0.5 rounded">Logout</button></div><textarea value={p} onChange={e => setP(e.target.value)} rows={2} className="w-full bg-[#060a14] p-2 text-xs text-white rounded border border-slate-800" /><button onClick={() => setDone(true)} className="w-full py-2 bg-cyan-600 font-bold text-xs rounded-xl text-white">🚀 Generate</button>{done && (<div className="space-y-2 pt-2 border-t border-slate-800">{scs.map(s => (<div key={s.id} className="bg-[#0b1222] border border-slate-800 p-2.5 rounded-xl space-y-1.5"><span className="text-[11px] font-bold text-cyan-400">{s.t}</span>{eid === s.id ? (<div className="space-y-1"><textarea value={etxt} onChange={e => setEtxt(e.target.value)} className="w-full bg-[#060a14] p-1 text-xs text-white rounded border border-cyan-500" /><div className="flex gap-1"><button onClick={() => { setScs(scs.map(x => x.id === s.id ? { ...x, desc: etxt } : x)); setEid(null); }} className="flex-1 py-1 bg-emerald-600 text-[9px] font-bold rounded">Save</button><button onClick={() => setEid(null)} className="px-2 py-1 bg-slate-800 text-[9px] rounded">Cancel</button></div></div>) : (<div className="flex justify-between items-center"><p className="text-[11px] text-slate-300">{s.desc}</p><button onClick={() => { setEid(s.id); setEtxt(s.desc); }} className="px-2 py-1 bg-slate-800 text-[9px] font-bold rounded">✏️ Edit</button></div>)}</div>))}</div>)}</div>); }
